# scripts/check.conf — steps behind ./scripts/check
# Stack: C# / .NET 8 (see docs/conventions.md).
#
# Greenfield note: no .sln/csproj exists yet (bootstrap does not write application code —
# see AGENTS.md invariant #1, "no spec, no code"). These are the real commands to activate,
# uncommented, as part of the FIRST feature spec that scaffolds the RoomBook solution:
#
# build: dotnet build --nologo -warnaserror
# test: dotnet test --nologo
#
# Until then this file has zero active steps, so ./scripts/check is a documented no-op.
